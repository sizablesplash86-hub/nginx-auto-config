cmake_minimum_required(VERSION 3.10)

# Define project name and language
project(NAP C)

# Set the C standard (C11 is standard, adjust if needed)
set(CMAKE_C_STANDARD 11)
set(CMAKE_C_STANDARD_REQUIRED ON)

# Option A: Option 1 approach (Single executable compiled from multiple sources)
add_executable(nap start.c presets.c)

# Option B: Option 2 approach (If you want two separate executables)
# add_executable(start start.c)
# add_executable(presets presets.c)




# 1. Create a dedicated build directory to keep source code clean
mkdir build && cd build

# 2. Generate the build files (Makefile, Ninja, etc.)
cmake ..

# 3. Compile the code
cmake --build .


to update:

#removes binary
cd /mnt/code-projects/packages/auto-config/'version 3.0'/build
rm -f /mnt/code-projects/packages/auto-config/'version 3.0'/nap-pkg/usr/local/bin/auto-config
#change the version release in the line
nano /mnt/code-projects/packages/auto-config/'version 3.0'/nap-pkg/DEBIAN/control
#compiles to the build creator thing
cmake --build .
mv nap /mnt/code-projects/packages/auto-config/'version 3.0'/nap-pkg/usr/local/bin/nginx-auto
#change for respective version
mkdir -p /mnt/code-projects/packages/auto-config/repository/releases/v3.0.0
#output to releases      change per version
dpkg-deb --build /mnt/code-projects/packages/auto-config/'version 3.0'/nap-pkg /mnt/code-projects/packages/auto-config/repository/releases/v3.0.0/auto-config_3.0.0_amd64.deb

git status
git add .
#change with what added
git commit -m "working on things"

cd /mnt/code-projects/auto-config
git status
git add .
#change with what added
git commit -m "working on things"

git push origin main